# lotus_core

Código cuja propriedade é do Lotus e que deve permanecer independente do
export FlutterFlow.

- `domain`: modelos e contratos puros;
- `application`: casos de uso e regras;
- `infrastructure`: serviços externos e implementações de repositórios.

A UI e os adaptadores do FlutterFlow não pertencem a este pacote.
